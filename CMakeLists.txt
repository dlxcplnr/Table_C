cmake_minimum_required(VERSION 2.8) # Checkout version of CMake.
									# If current version is older than
									# the specified one, an emergency exit 
									# will occur

add_executable(debug LR1_Ex5.c)	# Creates an executable file named 'debug'
				# from source LR1_Ex5.c
